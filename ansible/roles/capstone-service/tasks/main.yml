- name: Copy service configuration
  copy:
    src: "{{ ENVIRONMENT_NAME }}-service.yaml"
    dest: "/home/ubuntu"

- name: Create service configuration
  shell: "{{ kubectl_path }} apply -f {{ ENVIRONMENT_NAME }}-service.yaml --kubeconfig=~/.kube/config"
  args:
    chdir: "/home/ubuntu"
